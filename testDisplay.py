import displayVirusSpread as disp

displayData = disp.dataToDisplay()
displayData.startNodes.append(1)
dispplayData.typeOfGraph = disp.graphType.STAR


for node in displayData2.startNodes:
  print(node)